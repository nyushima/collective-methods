#### Scrapping twitter users' number of followers / posts every 15 minutes
------
Using beautiful soup

````python
from bs4 import BeautifulSoup
import urllib2
import time
from datetime import datetime

print('This script monitors the new tweets and changes in these twitter accounts')
print('@danielscioli, @mauriciomacri, @FPV_Argentina, @proargentina, @SergioMassa, @CFKArgentina')

twitterurls = [
"https://twitter.com/danielscioli", 
"https://twitter.com/mauriciomacri", 
"https://twitter.com/FPV_Argentina", 
"https://twitter.com/proargentina", 
"https://twitter.com/SergioMassa", 
"https://twitter.com/CFKArgentina"
]

def doevery15mins():
    for url in twitterurls:
        opener = urllib2.build_opener()
        opener.addheaders = [('User-agent', 'Mozilla/5.0 (Windows NT 6.1) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/41.0.2228.0 Safari/537.36')]
        response = opener.open(url)


        page = response.read()
        soup = BeautifulSoup(page, "html.parser")

        tweets = soup.find("a", {"data-nav": "tweets"})['title']
        followers = soup.find("a", {"data-nav": "followers"})['title']
        
        print url
        print str(datetime.now())
        print tweets
        print followers
        print
        
    
    time.sleep(900)

while True:
    doevery15mins()
````

#### Getting Twitter trends every 15 mins using `twitter` module
------
Customer, oauth key/secret omitted. 
````python
import twitter
import json
from urllib import unquote
import time

CONSUMER_KEY = 'omittedomittedomittedomitted'
CONSUMER_SECRET ='omittedomittedomittedomitted'
OAUTH_TOKEN = 'omittedomittedomittedomitted-omittedomittedomittedomitted'
OAUTH_TOKEN_SECRET = 'omittedomittedomittedomitted'

auth = twitter.oauth.OAuth(OAUTH_TOKEN, OAUTH_TOKEN_SECRET,
                           CONSUMER_KEY, CONSUMER_SECRET)

twitter_api = twitter.Twitter(auth=auth)

print twitter_api

WORLD_WOE_ID = 1
ARGENTINA = 23424747

world_trends = twitter_api.trends.place(_id=WORLD_WOE_ID)
arg_trends = twitter_api.trends.place(_id=ARGENTINA)

def doevery15mins():
    print arg_trends
    print json.dumps(arg_trends, indent=1)
    time.sleep(900)
    
    
while True:
    doevery15mins()

````

#### Twitter streamer using Node, Express, socket.io and node-twitter
------
Based on the [socket.io tutorial](http://socket.io/get-started/chat/) and example code of [node-twitter](https://www.npmjs.com/package/twitter)
Customer, oauth key/secret omitted

````javascript
var app = require('express')();
var Twitter = require('node-twitter');
var http = require("http");
var httpserver = http.createServer(app);
var io = require('socket.io').listen(httpserver);

app.get('/', function(req, res){
  res.sendFile(__dirname + '/index.html');
});

io.on('connection', function(socket){
  console.log('a user connected');
  socket.on('disconnect', function(){
    console.log('user disconnected');
  });
});

httpserver.listen(3000, function(){
  console.log('listening on *:3000');
});

var twitterStreamClient = new Twitter.StreamClient(  //this needs to be filled in before using
    'consumerkeyhere',
    'consumersecrethere',
    'accesstokenhere',
    'accesssecrethere'
);

twitterStreamClient.on('close', function() {
    console.log('Connection closed.');
});
twitterStreamClient.on('end', function() {
    console.log('End of Line.');
});
twitterStreamClient.on('error', function(error) {
    console.log('Error: ' + (error.code ? error.code + ' ' + error.message : error.message));
});
twitterStreamClient.on('tweet', function(tweet) {
    console.log(tweet.text);
    io.emit('incoming tweet', tweet.text);
});

twitterStreamClient.start(['#MacriPresidente', 'scioli', 'macri', 'balotage']);

````
